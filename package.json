{
  "name": "cougar-system",
  "version": "1.0.0",
  "private": true,
  "description": "40 SAR Cougar Company data system — tests for the multi-device sync layer.",
  "scripts": {
    "test": "node test/run.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:all": "node test/run.js && playwright test",
    "test:live": "node test/live/api-contract.test.js",
    "migrate": "node scripts/migrate-from-sheets.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "postgres": "^3.4.4"
  }
}
