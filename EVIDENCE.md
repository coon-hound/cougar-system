# Evidence — intake-migration

_Generated by `scripts/verify.sh`. Base: `origin/feat/supabase-backend`._

## Changed files
```
 .gitignore                          |    4 +
 EVIDENCE.md                         | 1018 +----------------------------------
 docs/INTAKE-MIGRATION.md            |  176 ++++++
 docs/NOMINAL-ROLL.md                |  129 +++++
 docs/nominal-roll-template.csv      |    6 +
 scripts/intake-migrate.mjs          |  447 +++++++++++++++
 scripts/intake-plan.mjs             |  770 ++++++++++++++++++++++++++
 supabase/migrations/0004_intake.sql |  374 +++++++++++++
 test/intake.test.js                 |  502 +++++++++++++++++
 9 files changed, 2410 insertions(+), 1016 deletions(-)
```

## node test/run.js — units + static guards
```

# backend: revisions + OCC
  ok   - getRev seeds to 1, bumpRev increments
  ok   - upsert with matching baseRev applies + bumps
  ok   - upsert with stale baseRev APPLIES (row-scoped, not rejected)
  ok   - full write (replace) with stale baseRev IS rejected (catastrophe path)
  ok   - missing baseRev applies (backward-compat) + still bumps
  ok   - append never conflicts even when far behind
  ok   - ensureColumnsForKeys adds a missing column on upsert
  ok   - deleteRowById removes the row and bumps

# backend: readTab double-read optimization
  ok   - getDisplayValues NOT called for a date-free tab
  ok   - getDisplayValues IS called when a time-only Date cell exists

# backend: revCheck + readAll carry revisions
  ok   - revCheck returns per-tab revs; readAll includes revs
  ok   - unauthorized request is rejected

# backend: lock isolation (tgPoll must never block data writes)
  ok   - data write SUCCEEDS while the script lock is held (tgPoll mid-poll)
  ok   - data write gets 503 (not applied, no bump) while the DOCUMENT lock is held
  ok   - data writes acquire ONLY the document lock
  ok   - tgPoll acquires ONLY the script lock, and bails silently when it's held
  ok   - unbound script fails LOUD, not as a misleading 503
  ok   - bot Medical append (tgCompleteMC path) bumps the rev atomically via withRevLock

# backend: applyOps (batched granular mutations)
  ok   - mixed ops apply IN ORDER with exactly one rev bump and aligned results
  ok   - one bad op does not abort the rest (partial success still bumps once)
  ok   - all-failed batch returns {error} and does NOT bump
  ok   - empty and oversized batches are rejected without a bump

# backend: append idempotency (retry-safe)
  ok   - appendRow with an existing id is a no-op (no duplicate row)
  ok   - appendMany skips rows whose id already exists (retried batch)
  ok   - applyOps append op is id-idempotent (batched replay adds nothing)

# backend: build stamp + non-ok logging
  ok   - every response carries the BACKEND_BUILD stamp (ping, revCheck, write, 401)
  ok   - non-ok responses are warn-logged with action+tab+code, ok responses are not

# helpers: outOfCampMap / isBookedOut (shared in/out-of-camp)
  ok   - active MC counts as out of camp (kind=medical)
  ok   - consume-in-camp MC does NOT count as out of camp
  ok   - LD does NOT count as out of camp (restricted but in camp)
  ok   - active leave counts as out of camp (kind=leave)
  ok   - booked out TODAY counts; YESTERDAY auto-clears
  ok   - medical takes precedence over a same-day book-out
  ok   - isBookedOut: day-scoped + boolean/text coercion

# helpers: statusRun (back-to-back re-issued statuses)
  ok   - adjacent re-issues merge into one run
  ok   - a gap keeps two absences separate
  ok   - an overlapping re-issue does not shorten the run
  ok   - away and consume-in-camp MCs never merge
  ok   - outOfCampMap carries the merged until/back date
  ok   - recovery tag waits for the run to end, and prints once
  ok   - a copy of a record does not chain with its own original
  ok   - back-to-back leave of the same type is one absence

# helpers: IPPT multi-attempt series (drives the cross-conduct charts)
  ok   - ipptSeriesByRecruit: valid scores keyed by attempt; YTT + zero-run rows excluded
  ok   - ipptPairedCohort: only recruits with BOTH attempts, for any pair
  ok   - ipptNetDelta: latest taken vs first taken; single attempt = 0

# helpers: nextId — a collision-free TEXT id
  ok   - nextId returns a string, never a number
  ok   - 50,000 ids are all distinct — not probably distinct, distinct
  ok   - a fresh 'session' cannot re-mint an earlier session's ids
  ok   - nextId never looks like a legacy numeric id
  ok   - ids sort by creation time (the time prefix is monotonic base36)

# intake — name normalisation
  ok   - token order does not matter (Chinese names arrive either way)
  ok   - particles are ignored (BIN / B. / S/O)
  ok   - different people do not collide
  ok   - similarity is measured against the shorter name
  ok   - padD4 leaves an archive key alone
  ok   - archiveKey never double-stamps

# intake — reading the roll
  ok   - quoted fields, embedded commas, doubled quotes, CRLF
  ok   - a final row with no trailing newline is not dropped
  ok   - header aliases are accepted and unknown columns reported

# intake — the roll must be usable
  ok   - a duplicated 4D blocks
  ok   - a commander-range 4D blocks
  ok   - a row with no name blocks
  ok   - a blank spacer row is skipped, not an error

# intake — recognising who came back
  ok   - an all-new roll produces no returnees
  ok   - a returnee is matched by name and re-homed onto the new seat
  ok   - a carried row gets a fresh id so the original can archive under its own
  ok   - carried ids are deterministic across runs
  ok   - an exact pid match wins and needs no name agreement
  ok   - two known people with the same name block rather than guess
  ok   - an ambiguity is resolved by an override, not by order
  ok   - two roll rows matching one person demote BOTH
  ok   - an override of NEW forces a fresh identity
  ok   - a near-miss name BLOCKS and asks for an explicit decision
  ok   - --accept-fuzzy downgrades it to a warning
  ok   - two common tokens in common is NOT a match
  ok   - one shared token is not enough to be a match

# intake — what carries and what archives
  ok   - clinical and fitness history follows the person
  ok   - MSK copies carry no id — that table has none
  ok   - only future appointments carry
  ok   - commander leave survives; recruit leave does not
  ok   - cohort-scoped tables are archived even for a returnee
  ok   - a non-returnee's records are not carried
  ok   - an open medical status is closed at the cutoff and reported
  ok   - a status that already ended before the cutoff is left alone

# intake — the people registry
  ok   - every recruit on the new roll gets a pid
  ok   - a returnee keeps one pid and accumulates seats
  ok   - an NRIC in the roll matches across a name change
  ok   - the raw NRIC never leaves the planner
  ok   - roster rows carry the pid and the intake stamp
  ok   - personal columns from the roll land on the roster row
  ok   - every roster row has an identical key set (the full schema)

# intake — the report
  ok   - a blocked plan says so and names the problem
  ok   - a clean plan invites the apply step
  ok   - returnees are named in the report so a human can check them

# issue-invites — padD4 (mirrors js/state.js:305)
  ok   - strips a leading C, upper or lower case
  ok   - left-pads 1-3 digit values to four — the commander case
  ok   - leaves a canonical 4D alone and trims stray whitespace
  ok   - accepts the number Sheets hands back, not just the string
  ok   - null / undefined / empty collapse to empty, never to '0000'
  ok   - a non-numeric value passes through untouched rather than being mangled

# issue-invites — inviteUsable
  ok   - an open invite is usable
  ok   - a revoked invite is not usable even with uses left and time left
  ok   - a fully redeemed invite is not usable
  ok   - a multi-use invite stays usable until its uses run out
  ok   - an expired invite is not usable, and expiry is exclusive at the boundary
  ok   - a null expiry means no expiry, not instant expiry
  ok   - counts arriving as text from postgres compare numerically
  ok   - timestamps arriving as ISO strings are compared as dates
  ok   - a missing invite is not usable — no invite is not a free pass

# issue-invites — inviteStatus (four distinct states)
  ok   - open
  ok   - redeemed
  ok   - expired
  ok   - revoked
  ok   - a revoked invite reads 'revoked', not 'redeemed' — the whole point of revoked_at
  ok   - revocation outranks redemption and expiry when several apply
  ok   - 'open' and 'usable' are the same claim — they can never disagree
  ok   - status defaults to the real clock when no 'now' is passed

# normalizers: booleans survive a text-typed backend
  ok   - appointments: the string "false" does not read as resolved
  ok   - appointments: TRUE in any casing reads as true
  ok   - appointments: real booleans are untouched (Sheets is unaffected)
  ok   - appointments: d4 is padded like every other layer
  ok   - appointments: a null row is passed through, not dereferenced

# normalizers: the same coercion, already in place elsewhere
  ok   - roster outOfCamp / campIn take text or boolean
  ok   - medical inCamp takes text or boolean
  ok   - roster leaveQuota: empty stays empty, and never becomes 0

# normalizers: row ids are TEXT on every layer
  ok   - medical: a NUMBER id from the backend comes out a STRING
  ok   - medical: a blank or null id becomes ""
  ok   - leave: a NUMBER id from the backend comes out a STRING
  ok   - leave: a blank or null id becomes ""
  ok   - attendance: a NUMBER id from the backend comes out a STRING
  ok   - attendance: a blank or null id becomes ""
  ok   - conductDetail: a NUMBER id from the backend comes out a STRING
  ok   - conductDetail: a blank or null id becomes ""
  ok   - appointments: a NUMBER id from the backend comes out a STRING
  ok   - appointments: a blank or null id becomes ""
  ok   - ippt: a NUMBER id from the backend comes out a STRING
  ok   - ippt: a blank or null id becomes ""
  ok   - rm: a NUMBER id from the backend comes out a STRING
  ok   - rm: a blank or null id becomes ""
  ok   - soc: a NUMBER id from the backend comes out a STRING
  ok   - soc: a blank or null id becomes ""
  ok   - polar: a NUMBER id from the backend comes out a STRING
  ok   - polar: a blank or null id becomes ""
  ok   - conducts: a NUMBER id from the backend comes out a STRING
  ok   - conducts: a blank or null id becomes ""
  ok   - a text id is passed through untouched (the post-cutover shape)

# normalizers: roster ids stay padded 4D strings
  ok   - normalizeRoster derives id via padD4, not normId
  ok   - a padded roster id auto-detects the commander role

# parade-compare: parser round-trips the real generator (lossless)
  ok   - header: type, DDMMYY date, time
  ok   - strength block round-trips exactly
  ok   - every person lands in the right section with a d4 key
  ok   - status label + duration parse into family/days/ISO range
  ok   - lossless: zero unparsed lines, zero warnings, confidence ours
  ok   - consume-in-camp MC parses with inCamp flag
  ok   - an MC extended by a second record parses as one MC run

# parade-compare: differ reports person-level events
  ok   - Pending → MC is ONE moved change, never an out+in pair
  ok   - MC extension shows as extended (+2D), not added+removed
  ok   - reason edit + leave return are reported
  ok   - strength deltas + movement reconcile (no warning)
  ok   - hand-edited strength line triggers the reconcile note
  ok   - self-diff and appended 'latest version as of' line are no-ops

# parade-compare: multiple entries per (person, section)
  ok   - resolved appointment is reported as removed, not swallowed
  ok   - newly booked second appointment is added, not a phantom edit
  ok   - a single rescheduled appointment still reads as a date edit

# parade-compare: foreign / partial formats
  ok   - foreign text: sections, ids and strength extracted best-effort
  ok   - rank change alone does not change the person key
  ok   - MED-only list vs full FP: only shared sections compared
  ok   - garbage paste fails the structured gate

# parade-compare: LCS line diff
  ok   - classic add/del/same

# parade-compare: snapshot row normalization (Sheets coercion)
  ok   - JSON-cell row round-trips; coerced legacy columns are repaired

# parade-compare: WhatsApp change summary
  ok   - summary text carries the headline groups

# parade: consume-in-camp MC placement + strength
  ok   - CURRENT STRENGTH counts the consume-in-camp recruit present
  ok   - ATTC lists only the ordinary away MC, not the in-camp one
  ok   - MEDICAL STATUS shows '(consume in camp)' and sorts MC above LD

# parade: a person is never in both ATTC and OTHERS
  ok   - active away MC + accidental book-out + leave → ATTC only
  ok   - borderline returnee (MC ended yesterday, ticked) + leave → ATTC only

# parade: chained statuses report the whole run
  ok   - ATTC states the extended MC's real end date and total days
  ok   - a genuine gap is NOT merged into one run
  ok   - OTHERS spans back-to-back leave of the same type
  ok   - strength still counts each body once across a run

# row ids: nextId is unique across devices
  ok   - nextId returns a string, not a number
  ok   - 50k ids in a tight loop are all distinct
  ok   - two independent 'devices' do not collide
  ok   - an id never collides with a legacy numeric id under ===

# row ids: normalizers pin ids to strings at the read boundary
  ok   - normalizeMedical (medical) turns a numeric id into a string
  ok   - normalizeLeave (leave) turns a numeric id into a string
  ok   - normalizeAttendance (attendance) turns a numeric id into a string
  ok   - normalizeConductDetail (conductDetail) turns a numeric id into a string
  ok   - padD4OnLayer (ippt/rm/soc/polar/appointments/conducts) turns a numeric id into a string
  ok   - a null or missing id becomes the empty string, never null
  ok   - a text id survives untouched, trimmed
  ok   - the comparison that broke: normalized id === editId from the DOM
  ok   - roster ids stay padD4-canonical strings

# static: load-time guards
  ok   - found the frontend scripts in index.html
  ok   - all scripts parse together (no duplicate top-level declarations)
  ok   - all script tags use the same ?v= cache version

# static: no unbumped tracked-tab writes (heuristic)
  ok   - direct tracked-tab writes are followed by a bumpRev

# static: lock-domain split (script lock is Telegram-only)
  ok   - every getScriptLock call is inside the Telegram section
  ok   - getDataLock uses the document lock with no script-lock fallback

# static: deploy observability
  ok   - BACKEND_BUILD exists and jsonResponse stamps it
  ok   - every mutating doPost branch is wrapped in withRevLock

# static: row ids are TEXT, and inline handlers quote them
  ok   - no submit handler coerces the edit id with +
  ok   - every row id interpolated into an inline handler is quoted

# sync-retry: 503 busy handling
  ok   - 503 twice then ok: in-place retry, no GET, growing backoff
  ok   - persistent 503: dirty + scheduled retry, heals when server recovers

# sync-retry: offline / online
  ok   - offline parks the op with no timer; online replays it
  ok   - online NetError blip: one in-place retry, then success

# sync-retry: 401 sign-in state
  ok   - 401: one POST total, queue stashed, authRestored replays all

# sync-retry: dirty-op persistence
  ok   - dirty ops persist across reload and replay granular, not replace
  ok   - corrupt / newer-version dirty-ops payload is discarded cleanly

# sync-retry: applyOps batching
  ok   - burst of upserts → ONE applyOps POST, ordered + coalesced
  ok   - old backend ('Invalid request'): falls back to per-row, all land
  ok   - partial batch failure: good ops land, bad op stashed + parked for background retry
  ok   - a later success does not erase an earlier still-pending failed op
  ok   - a later success to the SAME row evicts the stale stashed edit

# sync-retry: replace-conflict banner
  ok   - stale replace → banner with 'Push mine anyway' pushes the snapshot
  ok   - stale replace → dismiss keeps the server version

# sync-retry: launch never overwrites unsynced edits
  ok   - launch does not pull over a dirty tab; the local edit survives

# sync: concurrency (real frontend ↔ real backend)
  ok   - two devices edit different rows → both save, no conflict
  ok   - stale full replace rejected; B's row survives
  ok   - append applies even when far behind
  ok   - interleaved different-row writes from two devices never go dirty

# sync: auto-refresh
  ok   - autoRefreshTick pulls only the changed tab
  ok   - dirty tab protected from auto-refresh overwrite

# sync: propagation of non-app writes
  ok   - hand edit + onEditBumpRev propagates to other tabs
  ok   - bot Medical append + bumpRev propagates
  ok   - append WITHOUT bump is missed (documents why the bump is required)

# sync: recovery from a stuck 'unsaved' state
  ok   - retryAllDirty self-clears a stale dirty tab
  ok   - forceResync discards dirty + reloads authoritative state

# sync: queue + launch
  ok   - two rapid upserts both land (queue batches them)
  ok   - launch does revCheck + partial pull (no readAll) when baselined
  ok   - launch falls back to full readAll with no baseline

# verify-migration: the gate can actually fail on id not being a key
  ok   - (a) a collapsed duplicate-id pair FAILS — the old gate passed it
  ok   - (b) a dropped blank-id row FAILS — the old gate passed it
  ok   - (c) a clean tab PASSES, with every deliberate difference still exempt
  ok   - blank-id rows that all survive are noted but do not fail
  ok   - duplicate ids on the NEW side are reported too
  ok   - a surviving duplicate pair on both sides is compared row for row
  ok   - missing and extra keyed rows still fail, with duplicate counts
  ok   - NO_ID tabs are compared by raw count alone
  ok   - values stay withheld unless --show-values is passed
  ok   - the deliberate-difference exemptions are exactly the documented ones

# verify-migration: the id-map lets a reassigned row be followed, not excused
  ok   - (m1) a clean mapped tab PASSES, and says how each row was matched
  ok   - (m2) a blank-id row the mapping MISSES still fails
  ok   - (m3) a mapping that reuses a newId FAILS loudly
  ok   - (m4) a mapped row whose OTHER fields changed still FAILS
  ok   - (m5) a newId colliding with a PRESERVED id in the same tab FAILS
  ok   - (m6) a mapping that does not describe this backup FAILS
  ok   - (m7) raw row counts stay absolute — the mapping cannot excuse a lost row
  ok   - (m8) with NO id-map.json the gate behaves exactly as before
  ok   - (m9) an id-map.json that exists but is unusable FAILS loudly, never as 'no mapping'
  ok   - (m10) a well-formed id-map.json round-trips off disk

252 passed, 0 failed
```

## test/live — the real Edge Function over HTTP
```
# live API contract against http://127.0.0.1:8000/

# live API: reads
  ok   - ping is the only public action; everything else 401s
  ok   - readAll returns every tracked tab, keyed as the client expects
  ok   - read&tab returns {rows, rev}
  ok   - revCheck agrees with readAll
  ok   - empty cells come back as "", never null

# live API: row-scoped writes
  ok   - a partial upsert does not blank the columns it omits
  ok   - encrypted columns round-trip as plaintext
  ok   - deny-listed fields are dropped, not parked in `extra`
  ok   - a roster row carrying only "4d" gets the canonical id derived
  ok   - row-scoped writes ignore a stale baseRev

# live API: append idempotency
  ok   - a replayed append creates no duplicate and does not bump the rev
  ok   - appendMany counts appends, not rows accepted
  ok   - an all-duplicate appendMany is a noop that does not bump the rev
  ok   - appendMany rejects an empty batch the way the old backend did

# live API: applyOps batching
  ok   - applyOps applies in order, one rev bump for the whole batch
  ok   - one bad op fails alone; the rest of the batch still lands
  ok   - a delete is soft, and a replayed delete is a noop
  ok   - re-appending a deleted id revives it and reports an append

# live API: full-tab write + OCC
  ok   - a stale full-tab write is rejected without mutating anything
  ok   - the same write against a fresh baseRev is applied
  ok   - a full-tab write soft-deletes the rows it omits
  ok   - an empty full-tab write is refused, not obeyed
  ok   - a full-tab write never destroys a column the payload omits

# live API: tabs with no `id` column
  ok   - MSK takes a full-tab replace
  ok   - id-based ops on MSK give the old backend's error, not raw SQL
  ok   - Config stays a single row however it is written
  ok   - untracked tabs carry no revision

# live API: response envelope
  ok   - every response is HTTP 200 and carries `build`
  ok   - an unsupported action answers the literal 'Invalid request'
  ok   - rowCount reports live rows only
  ok   - a Bearer token authenticates as well as the legacy auth param

# live API: cleanup
  ok   - test fixtures are removed

32 passed, 0 failed
```

## Manual test in the website
```
bash scripts/preview.sh intake-migration 8080   # then open the printed URL
```

## Result: ✅ PASS
